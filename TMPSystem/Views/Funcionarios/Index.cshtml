@model IEnumerable<GestaoProducao_MVC.Models.Funcionario>

@{
    ViewData["Title"] = "Funcionários";
}

<h1 class="text-center mt-4">@ViewData["Title"]</h1>

<div class="menuIndex">
    <a asp-controller="Apontamentos" asp-action="CentralApontamentos" class="btn btn-outline-dark">Retornar para a central</a>
    <a asp-action="Create" class="btn btn-outline-dark">Adicionar um funcionário</a>
</div>
@*<form asp-action="Index" method="get">
    <p>
        <input type="text" name="SearchString" class="form-control-sm" />
        <input type="submit" value="Buscar" class="btn btn-sm btn-outline-info" />
        <a asp-action="Index" class="btn btn-sm btn-outline-dark">Listar todos</a>
    </p>
</form>*@
<table class="table table-hover table-striped cell-border" id="tabela-funcionarios">
    <thead>
        <tr class="table-active">
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cargo)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr class="table-active">
        <td>
            @Html.DisplayFor(modelItem => item.Id)
        </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cargo)
            </td>
          </tr>
}
    </tbody>
</table>
